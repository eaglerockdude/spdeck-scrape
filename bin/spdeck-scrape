#!/usr/bin/env ruby

require 'spdeck-scrape'

# usage: $ spdeck-scrape my_query [verbose | concise] html[optional for html gen]

user = SpeakerdeckScraper.new(ARGV[0], ARGV[1])
user.scrape_all
user.html_gen if ARGV[2] == "html"